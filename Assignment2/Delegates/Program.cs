using System.Security.Cryptography.X509Certificates;
using static Delegates.Delegate;
internal class Program
{
    
    private static void Main(string[] args)
    {

        // Delegate

        //MyDelegate multiply = Multiply;
        //MyDelegate substract = Substract;

        //Console.WriteLine(OperationToPerform(substract, 5, 5));
        //Console.WriteLine(OperationToPerform(multiply, 5, 5));


    }
    public void hello(string name) {  Console.WriteLine(name); }
}